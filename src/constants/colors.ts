import { Gamut, GamutResult } from "@daanv2/hue";

export namespace Colors {
  export const SOFT_RED: GamutResult = Gamut.fromRGB([215, 40, 85]);
  export const GOLD: GamutResult = Gamut.fromRGB([215, 150, 40]);
}
